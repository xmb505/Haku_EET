- 配置拆分为 config.yaml / io_config.yaml / display_config.yaml / ui_config.yaml 四份，分别对应大脑、脑干、显示编码与 UI 映射，避免单文件膨胀
- 启动参数一律通过 argparse 暴露 --config/--io-config/--display-config/--simulate，默认值指向 config/ 下同名 yaml